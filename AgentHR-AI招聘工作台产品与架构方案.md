# AgentHR AI 招聘工作台产品与架构方案

> 方向提案｜2026-09-15
> 本文用于明确 AgentHR 从“带 Agent 的招聘浏览器”升级为“以 DSH 为智能核心的招聘工作台”的产品方向、信息架构、技术边界与迁移路径。

## 1. 结论

AgentHR 不应以招聘网站浏览器作为默认主界面，而应成为一个面向招聘人员的 AI 工作台：

- DSH Agent 是用户的主要交互入口。
- 岗位、候选人、人才库、任务和招聘记录是产品的核心业务对象。
- BOSS 直聘、猎聘等网站是 Agent 执行招聘任务时按需使用的外部工具。
- 招聘网站浏览器默认收起，需要登录、检索、查看简历或沟通时再从右侧展开。
- Agent 的判断、浏览器动作和关键结果必须可观察、可复核、可追溯。

产品应从：

> 浏览器是主体，DSH 是右侧助手

调整为：

> AI 招聘工作台是主体，浏览器是 Agent 按需调用的执行工具

## 2. 为什么需要调整

当前原型采用左侧招聘网站、右侧 DSH 对话的固定分栏。这个结构适合快速验证 Electron 内置浏览器、DSH 工具调用和招聘网站适配，但存在明显的产品认知问题：

1. 浏览器占据主要视觉空间，用户容易把 AgentHR 理解为“套壳招聘网站”。
2. 岗位与分析记录被压缩在次级窄栏，无法承载完整招聘流程。
3. 用户离开招聘网页后，缺少一个稳定的业务空间继续管理候选人、任务与结论。
4. DSH 对话虽然具备决策能力，但没有成为贯穿岗位、候选人和任务的统一工作入口。
5. 多个平台的价值没有沉淀到 AgentHR 自己的数据模型中，容易退化为对多个网站的简单切换。

新版工作台需要让用户首先看到“今天要完成什么、当前有哪些岗位、哪些候选人待处理”，而不是首先看到某个招聘网站。

## 3. 产品定位

### 3.1 一句话定义

AgentHR 是以 DSH 为智能核心的桌面招聘工作台，通过按需展开的招聘浏览器执行跨平台招聘任务，并将过程与结果沉淀为岗位、人才和可追溯记录。

### 3.2 核心价值

- **统一入口**：用户通过自然语言发起招聘工作，无需先判断应进入哪个平台和页面。
- **跨平台沉淀**：来自不同招聘网站的信息统一进入岗位、人才库和任务记录。
- **证据化判断**：候选人结论必须关联岗位条件和简历原文，区分明确证据、相关线索、未知和明确不符。
- **人机协作**：Agent 负责搜索、整理、分析和生成草稿；招聘人员负责关键确认、沟通和最终决策。
- **过程可见**：网页操作、候选人身份、数据来源、失败原因和待确认动作均可查看。

### 3.3 第一阶段边界

- 支持岗位创建、编辑和切换。
- 支持猎聘与 BOSS 直聘的登录、搜索、候选人读取和详情打开。
- 支持候选人证据分析、问题草稿和人工复核。
- 支持人才库和任务记录的基础沉淀。
- 不自动发送候选人消息。
- 不替招聘人员作最终录用决定。
- 不宣称尚未通过真实账号验证的平台操作已经稳定。

## 4. 产品信息架构

### 4.1 一级导航

| 模块 | 主要职责 |
|---|---|
| AI 工作台 | DSH 对话、当前任务、上下文和 Agent 执行状态 |
| 岗位管理 | 岗位、JD、技能条件、薪资、地点、状态和招聘目标 |
| 人才库 | 跨平台候选人、来源、标签、阶段、分析、备注和历史 |
| 招聘任务 | 搜索、筛选、分析、待确认动作、进度和错误 |
| 招聘记录 | Agent 操作、人工操作、证据、结论和状态变化 |
| 平台账号 | BOSS、猎聘等平台的登录状态、Profile 和可用性 |
| 设置 | 模型、权限、数据、安全策略和应用配置 |

“职位管理”和“岗位管理”不应作为两个并列概念，产品中统一使用“岗位管理”。英文的岗位描述使用 **Job Description（JD）**。

### 4.2 桌面布局

```text
┌──────────────┬──────────────────────────────────┬─────────────────────────┐
│ 一级导航      │ 中央主工作区                       │ 右侧上下文面板             │
│              │                                  │                         │
│ AI 工作台     │ DSH 对话 / 当前任务                │ 招聘网站浏览器             │
│ 岗位管理      │ 岗位详情 / JD                     │ 候选人详情                 │
│ 人才库        │ 候选人列表 / 分析                  │ 证据与来源                 │
│ 招聘任务      │ 任务进度 / 待处理项                 │ Agent 操作记录             │
│ 招聘记录      │ 历史记录 / 审批                    │ 待确认动作                 │
│ 平台账号      │                                  │                         │
└──────────────┴──────────────────────────────────┴─────────────────────────┘
```

中央主工作区是产品主体。右侧面板根据当前上下文显示浏览器、候选人详情、证据或任务日志，不永久占据空间。

### 4.3 AI 原生业务交互

岗位、人才库、任务和记录不能只是传统表单页面。它们既是可视化业务页面，也是 DSH 可以通过对话读取和维护的结构化业务对象。

用户不必先找到某个页面再逐项填写，可以直接对 DSH 表达意图：

- “帮我新建一个上海的 Java 后端岗位，需要 5 年以上经验，预算 30K 至 45K。”
- “把刚才补充的微服务经验加入这个岗位，但不要设成硬性条件。”
- “把今天看到的三名候选人加入这个岗位的人才池，并标记其中两个需要确认薪资。”
- “李明已经完成一面，把他移动到待二面，并记录面试官反馈。”
- “汇总本周还没有下一步安排的候选人，帮我生成跟进任务。”

DSH 应把自然语言转成结构化变更，通过 AgentHR 业务工具完成读取、校验和写入。成功后，对话中直接呈现岗位卡片、候选人卡片、任务清单或变更摘要，用户可以继续用自然语言修改，也可以打开对应页面精确编辑。

产品交互应遵循：

> 对话负责表达意图，业务卡片负责展示结果，结构化页面负责精确编辑，领域服务负责校验和持久化。

表单不会完全消失，但不再是唯一入口。它主要用于：

- 查看 Agent 解析后的完整结构。
- 修正薪资、日期、阶段、标签等精确字段。
- 对比修改前后内容。
- 补充模型无法可靠推断的信息。
- 审批高影响动作。

### 4.4 各业务模块的 AI 化能力

| 模块 | 对话式能力 | 可视化页面职责 |
|---|---|---|
| 岗位 | 从自然语言创建 JD、补充要求、区分硬性与加分条件、发现缺失信息 | 展示岗位完整结构、版本差异、状态和精确字段编辑 |
| 人才库 | 添加候选人、归类标签、关联岗位、总结经历、发现重复线索、生成跟进建议 | 候选人列表、详情、来源、阶段、关系和人工合并 |
| 任务 | 把招聘目标拆成搜索、分析、确认和跟进任务，汇报进度并处理异常 | 任务队列、执行状态、暂停、重试、审批和失败诊断 |
| 记录 | 自动沉淀页面动作、分析依据、状态变化和人工决定，并按需生成摘要 | 时间线、证据、审计、筛选和历史版本 |

AI 化不等于让模型任意修改数据。DSH 只能调用明确、强类型的领域工具，例如 `jobs.create`、`jobs.update`、`candidates.linkToJob`、`applications.changeStage` 和 `tasks.create`。领域服务负责字段校验、权限、版本冲突和审计记录，模型不能直接执行 SQL 或绕过业务规则。

### 4.5 变更确认分级

| 级别 | 示例 | 默认策略 |
|---|---|---|
| 只读 | 查询岗位、汇总候选人、查看任务 | Agent 可直接执行 |
| 低风险内部写入 | 新建岗位草稿、添加标签、创建内部任务、保存分析草稿 | 用户意图明确时可直接执行，并返回变更摘要和撤销入口 |
| 关键业务变更 | 修改岗位硬性条件、合并候选人、改变招聘阶段 | 展示结构化变更预览，确认后执行 |
| 外部影响动作 | 发送消息、邀请面试、操作招聘平台、导出候选人数据 | 执行前确认身份、对象、内容和范围 |
| 破坏性动作 | 删除岗位、删除候选人、批量清空记录 | 必须明确确认并保留审计信息 |

每次写入都应返回业务对象 ID、变更字段和最新版本。连续对话修改同一对象时携带版本号，避免 Agent 使用过期上下文覆盖用户刚刚完成的编辑。

## 5. 浏览器交互设计

### 5.1 浏览器的三种状态

招聘网站通常需要较大的页面宽度，不能只设计成狭窄侧栏。浏览器应支持三种显示状态：

| 状态 | 使用场景 | 建议占用空间 |
|---|---|---|
| 收起 | 浏览岗位、人才库、任务和对话 | 不占主工作区 |
| 分屏 | Agent 搜索、读取卡片、打开候选人 | 窗口宽度的 45% 至 60% |
| 全屏 | 登录、复杂筛选、查看完整简历、处理验证码 | 除全局导航外的主要空间 |

### 5.2 自动展开规则

以下动作可以触发浏览器展开：

- 用户主动点击“打开招聘网站”。
- Agent 需要用户登录或处理验证码。
- Agent 开始搜索候选人。
- Agent 请求用户确认当前候选人或页面状态。
- Agent 执行可能改变网页状态的操作。
- 页面适配失败，需要用户人工接管。

只读提取和后台等待不必强制展开，但界面必须显示 Agent 正在访问的平台、页面和候选人。

### 5.3 人工接管

浏览器面板必须始终支持人工接管：

- 暂停 Agent 页面操作。
- 手动完成登录、验证码和复杂筛选。
- 手动选择或关闭候选人详情。
- 将当前页面控制权交还 Agent。
- 查看最近一次 Agent 动作、目标元素和执行结果。

Agent 和招聘人员不得同时修改同一个招聘页面。浏览器操作应按平台 Profile 串行执行。

## 6. 典型工作流

### 6.1 创建岗位并寻找候选人

1. 用户在 AI 工作台描述招聘需求。
2. DSH 将自然语言整理为岗位名称、JD 和逐项技能条件。
3. 用户确认并保存岗位。
4. 用户要求 Agent 寻找候选人。
5. Agent 选择招聘平台并展开右侧浏览器。
6. Agent 搜索、读取当前结果并生成候选人线索。
7. 候选人线索与岗位关联后进入人才库。
8. Agent 打开候选人详情，生成有原文依据的分析草稿。
9. 用户在中央工作区复核分析，而不是继续停留在招聘网页中。

### 6.2 分析候选人

```text
岗位条件
  -> 读取候选人详情
  -> 按条件逐项匹配证据
  -> 标记明确证据 / 相关线索 / 未知 / 明确不符
  -> 生成需要确认的问题草稿
  -> 保存分析版本
  -> 人工复核
```

例如岗位要求“熟悉 tMCAO 动物造模”，而简历只写“动物实验”或“MCAO”时，只能标记为相关线索或未知，不能自动判定已经掌握 tMCAO。

### 6.3 后续沟通

后续版本可以增加候选人沟通，但必须遵循：

1. 重新确认平台、账号、岗位和候选人身份。
2. 重新读取最新会话状态。
3. Agent 生成消息草稿。
4. 用户审核或在明确配置的范围内授权发送。
5. 使用幂等键和频率限制防止重复发送。
6. 保存发送内容、时间、平台结果和后续回复。

## 7. DSH 的职责

DSH 是 AgentHR 的智能运行时，负责：

- 对话与会话管理。
- 理解招聘人员的自然语言需求。
- 选择和调用 AgentHR 业务工具。
- 结合岗位条件分析候选人。
- 生成问题、摘要、建议和任务计划。
- 将招聘人员的自然语言转换为岗位、候选人、任务和记录的结构化变更请求。
- 在对话中展示可继续操作的业务卡片和变更结果。
- 在任务执行过程中向用户说明状态和请求确认。

DSH 不应直接拥有 Electron 窗口，也不应获得任意页面脚本、Cookie、文件系统或数据库访问能力。它只调用 AgentHR 提供的受控业务工具。

建议保留独立的 AgentHR DSH preset 和插件，并把工具按业务领域组织：

- `jobs.*`：岗位读取、创建、更新和切换。
- `browser.*`：页面状态、快照、导航和受控动作。
- `candidates.*`：候选人线索、详情和身份校验。
- `assessments.*`：证据分析、草稿和人工状态。
- `tasks.*`：任务创建、进度、停止和恢复。
- `communications.*`：消息读取与草稿；发送能力后续单独授权。

业务对象的读取与修改必须形成统一闭环：

```text
用户自然语言
  -> DSH 理解意图并补齐必要参数
  -> 调用强类型 AgentHR 业务工具
  -> Domain Service 校验权限、版本和业务规则
  -> SQLite 事务写入业务数据与变更事件
  -> 工作台实时刷新
  -> DSH 返回业务卡片、变更摘要和下一步建议
```

DSH 对话不是业务数据库。岗位、人才和任务的最终状态以 AgentHR 领域服务为准；会话保存的是意图、解释和操作上下文。

## 8. GoodHR 的借鉴范围

### 8.1 建议借鉴

GoodHR 可以作为招聘领域和页面操作的参考，重点借鉴：

- 岗位配置和岗位运行模型。
- 招聘平台 URL、页面结构和选择器管理。
- 候选人搜索、列表读取、详情打开和简历提取流程。
- 点击、输入、滚动、截图和结果验证的动作拆分。
- 人才库、候选人阶段、备注和跨岗位关联。
- 自动筛选、待沟通、面试、淘汰和推荐等招聘状态。
- 任务日志、错误码、重试、暂停和恢复机制。
- Profile、Cookie、下载文件和敏感数据的本地边界。
- 候选人去重、消息幂等、频率限制和人工确认。

### 8.2 不直接沿用

- 不把 GoodHR 的 Go 服务作为 AgentHR 主运行时。
- 不引入 GoodHR 的云端控制台作为桌面工作台。
- 不把 CloakBrowser 作为当前版本的强制依赖。
- 不照搬固定批处理流程替代 DSH 的对话式任务编排。
- 不直接复制缺少许可核验或未经真实页面验证的代码。

GoodHR 提供领域经验和站点经验，AgentHR 自己负责产品体验、Agent 编排和 Electron 执行边界。

## 9. 推荐技术架构

```text
招聘人员
  -> AgentHR Desktop Shell
      -> React 招聘工作台
      -> DSH Web UI
      -> Browser Drawer (Electron WebContentsView)
      -> Electron Main Process
          -> DSH Host
          -> AgentHR Local Bridge
          -> Recruitment Adapters
          -> Job / Candidate / Task Services
          -> SQLite
```

### 9.1 模块边界

| 模块 | 职责 |
|---|---|
| Desktop Shell | 窗口、导航、布局、浏览器面板和原生生命周期 |
| DSH Host | Agent、模型、会话、工具调用和流式响应 |
| AgentHR Plugin | 招聘系统提示词、业务工具和 Agent 行为边界 |
| Local Bridge | 对 DSH 暴露带认证、强类型的本机业务接口 |
| Browser Controller | WebContentsView、Session、页面生命周期和输入事件 |
| Platform Adapter | BOSS、猎聘等平台的页面识别、提取和动作实现 |
| Domain Services | 岗位、候选人、分析、任务、沟通和记录 |
| SQLite | 本地业务数据和可追溯状态 |

Renderer 和 DSH 插件必须调用同一套 Domain Services。这样用户在页面表单中修改岗位，与通过 DSH 对话修改岗位，都会经过相同校验并产生相同的变更事件，不会形成两套业务逻辑。

### 9.2 不建议把 HR 功能直接写入 DSH 核心

DSH 应保持通用。岗位、人才库和招聘任务属于 AgentHR 产品层，应放在 AgentHR 的 Renderer、插件和本地业务服务中。这样可以：

- 独立升级 DSH。
- 独立测试招聘业务。
- 避免依赖 DSH Desktop 的内部 Electron API。
- 将来复用 DSH 的新会话、任务和插件能力。
- 保持招聘数据和页面权限边界清晰。

## 10. 数据模型建议

### 10.1 岗位 Job

- `id`
- `title`
- `description`
- `criteria[]`
- `salaryRange`
- `location`
- `employmentType`
- `status`
- `createdAt` / `updatedAt`

### 10.2 候选人 Candidate

- `id`
- `displayName`
- `currentCompany`
- `currentTitle`
- `location`
- `expectedSalary`
- `expectedPosition`
- `stage`
- `tags[]`
- `createdAt` / `updatedAt`

### 10.3 平台身份 CandidateSource

- `candidateId`
- `platform`
- `platformCandidateId`
- `profileId`
- `sourceUrl`
- `contentDigest`
- `lastSeenAt`

候选人与平台身份必须分离。一个候选人可以来自多个平台；只有在具备稳定平台 ID 或人工确认时才能合并，不能只凭姓名自动合并。

### 10.4 候选人与岗位关系 Application

- `candidateId`
- `jobId`
- `stage`
- `assessmentId`
- `owner`
- `nextAction`
- `nextActionAt`
- `createdAt` / `updatedAt`

同一个候选人可以对应多个岗位，并拥有不同的判断、阶段和跟进计划。

### 10.5 任务 RecruitmentTask

- `id`
- `jobId`
- `type`
- `status`
- `platform`
- `profileId`
- `progress`
- `currentCandidateId`
- `startedAt` / `finishedAt`
- `errorCode` / `errorMessage`

## 11. 安全与可信边界

- 招聘网站远程内容保持 `nodeIntegration: false`、`contextIsolation: true` 和 `sandbox: true`。
- DSH 工具不暴露任意 JavaScript 执行、Cookie 读取或通用文件访问。
- 页面文本按不可信输入处理，忽略其中要求 Agent 改变规则或泄露信息的指令。
- 所有写操作重新校验页面、元素、平台、候选人和任务上下文。
- 登录凭据由招聘网站 Session 管理，不写入项目文件、日志或测试快照。
- 候选人完整简历、页面截图和对话内容默认保存在本机。
- 发送消息、删除记录和合并候选人属于高影响动作，需要明确确认和审计记录。

## 12. 现有实现的复用与调整

### 12.1 可以保留

- DSH Host 的启动、关闭、重试和认证 URL 获取。
- AgentHR DSH preset 与招聘业务工具。
- 本机 Bearer Token 桥接。
- Electron `WebContentsView` 和独立持久 Session。
- BOSS 与猎聘平台适配器。
- 页面快照、临时元素引用和动作后重新观察机制。
- 岗位本地存储、分析记录和 SQLite。
- 候选人证据原文与岗位摘要指纹校验。

### 12.2 需要调整

- 将固定的“浏览器主区域 + 右侧 560px 面板”改为工作台主布局。
- 将 DSH 对话从窄栏提升为中央一级工作区。
- 将岗位和分析记录拆分为独立业务页面。
- 新增人才库、任务和平台账号页面。
- 将浏览器改为可收起、可分屏、可全屏的上下文面板。
- 把当前候选人卡片、简历详情和分析结果沉淀为正式领域对象。
- 增加 Agent 页面操作暂停、人工接管和交还控制权机制。

## 13. 分阶段迁移计划

### 阶段一：重构产品外壳

- 新增左侧一级导航。
- DSH 对话成为默认中央页面。
- 岗位管理和分析记录变成独立页面。
- 浏览器支持收起、分屏和全屏。
- 保持现有桥接、工具和适配器接口不变。
- 让对话中可以创建和更新岗位草稿，并返回可操作的岗位卡片。

验收标准：应用默认打开后不再像浏览器；不展开招聘网站也能通过 DSH 创建岗位、补充岗位信息、查看任务，并在结构化页面中看到一致结果。

### 阶段二：建立人才库与任务模型

- 增加 Candidate、CandidateSource、Application 和 RecruitmentTask。
- 将当前分析卡片迁移为候选人与岗位关系下的分析版本。
- 增加候选人列表、详情、阶段和备注。
- 增加任务进度、取消、错误和历史记录。

验收标准：浏览器收起后，用户仍能完整查看本次招聘任务产生的候选人和分析结果。

### 阶段三：真实平台验证

- 由招聘人员验证猎聘和 BOSS 登录保持。
- 校正搜索结果、卡片、详情、iframe 和滚动适配。
- 建立平台候选人稳定 ID 获取策略。
- 完善页面失败诊断和人工接管。

验收标准：两个平台的关键只读流程有可重复的真实账号验收记录。

### 阶段四：受控沟通

- 读取候选人会话。
- 生成消息草稿。
- 增加身份校验、人工审批、幂等和频率限制。
- 保存消息发送与回复记录。

验收标准：不会向错误候选人或错误平台发送消息，不会重复发送，所有消息可追溯。

## 14. 关键设计原则

1. **工作台优先**：岗位、候选人和任务优先于网站页面。
2. **Agent 优先但不越权**：Agent 负责理解和执行，关键动作由用户确认。
3. **浏览器工具化**：浏览器是可观察、可接管的执行器，不是产品首页。
4. **业务数据沉淀**：招聘结果必须进入 AgentHR 数据模型，不能只存在网页或聊天记录中。
5. **判断与动作分离**：模型决定做什么，受控执行层决定如何安全完成。
6. **证据优先**：候选人判断必须关联岗位条件、来源和原文。
7. **平台适配隔离**：页面结构变化只影响对应平台适配器。
8. **渐进自动化**：先只读和草稿，再人工确认，最后才考虑有限自动执行。
9. **对话与页面双入口**：所有核心业务既可由 DSH 对话操作，也可在结构化页面中查看和精确编辑。
10. **单一业务写入链路**：对话和界面共用领域服务、校验、事务与审计，不建立两套数据逻辑。

## 15. 最终产品形态

AgentHR 的长期形态不是一个增加了聊天栏的招聘网站浏览器，也不是 GoodHR 的简单桌面化版本，而是一个拥有自己业务数据和工作流的 AI 招聘工作台：

- DSH 提供对话、推理和任务编排。
- AgentHR 提供招聘领域模型、界面、权限和安全边界。
- GoodHR 提供可借鉴的招聘流程与站点操作经验。
- Electron 提供本地桌面壳、浏览器、登录 Session 和数据存储。
- 招聘网站作为外部执行环境按需展开。

最终用户关注的应是“这个岗位有哪些合适的人、哪些信息还需要确认、下一步做什么”，而不是“浏览器当前打开了哪个页面”。
